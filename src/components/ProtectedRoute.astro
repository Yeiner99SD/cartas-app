---
---

<script>
  function checkAuth() {
    const isLoggedIn = localStorage.getItem('isLoggedIn');
    const currentPath = window.location.pathname;
    
    // Verifica si no está logueado y está intentando acceder a rutas protegidas
    if (!isLoggedIn && (currentPath === '/home' || currentPath.startsWith('/cartas/') || currentPath.startsWith('/nuestras-cositas') )) {
      window.location.href = '/';
    }
  }

  // Verificar al cargar la página
  checkAuth();

  // Verificar cuando el estado de navegación cambie
  window.addEventListener('popstate', checkAuth);
</script>
